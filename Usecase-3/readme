Use case 3 : 

Create a custom image of ubuntu with JDK 8 installed, ping, VI editor, Mysql-client and MAVEN installed.
Deploy a java application in a docker container. Automatic integration of git with the docker hub repository.

1. Copy Docker file as is to the Eclipse Project
2. Commands to Execute

